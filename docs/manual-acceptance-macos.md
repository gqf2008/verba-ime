# macOS 手动验收清单（M6 · IMK）

> 目标：安装/启用 → 直输 → 候选窗 → // AI 流式 → OCR → 冻结能力负向验证。
> 口径：OCR=正式能力；ASR/TTS=实验性冻结（默认关闭、入口隐藏，M6 不验收效果）。
> 前置：`frontends/macos/ime` 构建 → `scripts/package.sh` → 装入 `~/Library/Input Methods` → 系统设置→键盘→输入法启用「拾言输入法」。

## 安装与启用
- [ ] Verba.app 装入 ~/Library/Input Methods，系统设置可见「拾言输入法」
- [ ] 切换输入法后 IMK 控制器激活（可输入）

## 直输
- [ ] 英文/数字/标点直输上屏（无残留 preedit）
- [ ] 退格删除、Esc 取消组合、Enter 提交

## 候选窗（Rime 单引擎）
- [ ] 拼音组合 → 候选窗自动展示（数字键/点击选择）
- [ ] ←/→ 翻页；主题（light/dark）与布局配置生效
- [ ] 中文候选上屏正确

## // AI 流式（LLM 核心链路）
- [ ] 输入 // → AI 模式 → 提示词 → Enter → 流式 preedit → Enter 上屏
- [ ] 流式中 Esc 取消无残留；快速流切换旧流不混入（5 轮连测）

## OCR（正式能力，P0）
- [ ] //截图 或截图入口 → 选区/屏幕捕获 → OCR 结果上屏（需屏幕录制权限 ScreenCaptureKit）
- [ ] 权限弹窗：拒绝→重试、授权→可用；无选区取消无残留

## 冻结能力负向验证（ASR/TTS）
- [ ] 设置中 ASR/TTS 默认关闭；入口隐藏（菜单/快捷键不可见或置灰）
- [ ] 触发热键不产生录音/合成行为（不承诺、不验收效果）

## 权限与发布
- [ ] 麦克风 TCC 弹窗（NSMicrophoneUsageDescription）行为确认
- [ ] Developer ID 签名 + 公证（发布前置，需证书）

## 判定
- P0/P1 清零、CI 全绿 → macOS IMK 验收通过

---

# macOS 手动验收清单（#87/#88/#89/#90 批次）

> 背景：快打漏字队列化（#87）、菜单栏图标撑爆（#88）、AI 交互重做（#89）、
> CapsLock 审查修复含 F10 行为变更（#90）。AI 三态按键与盲按队列的语义与
> Windows 同批共用（按键映射在共享 core），完整矩阵见
> `manual-acceptance-windows.md` 对应批次；此处列 macOS 侧专项。

## 菜单栏图标（#88）
- [ ] 输入法菜单栏图标高度恢复正常（18pt 模板画布，不再撑爆菜单栏）
- [ ] 深色 / 浅色菜单栏下模板渲染正确（TISIconIsTemplate）

## CapsLock 现场（#90，含行为变更 F10）
- [ ] 系统「CapsLock 切换 ABC」开启：切英文瞬间候选/预览/组合清理，无粘滞、
      无残留面板
- [ ] OCR / 改写预览跨 CapsLock 切换不粘滞（切走再切回预览已清理）
- [ ] **F10 行为变更**：OCR 预览显示时按 `2` 不再选取（`2` 仅在改写对照预览
      下取原文）；OCR 预览用 `1`/Enter 选取

## AI 结果浮层与三态按键（#89 一期形态：IMKCandidates 面板）
- [ ] 流式期间标记文本只显示「✨ 生成中…」，候选面板显示截断结果（40 字符）
      + 状态行；生成完成 → 「✨ 已就绪」+ 就绪提示行
- [ ] 就绪态 Enter/空格/`1` 上屏**全文**（面板截断只影响显示）、`r` 重试、
      `e` 改提示词、Esc 关闭
- [ ] 失败态浮层保留：Enter 或 `r` 重试、`e` 改提示词、Esc 关闭——错误不得
      一闪而过
- [ ] `e` 回提示词编辑时残留 AI 面板收起（提示行条目不可再点击）
- [ ] 改写预览选取（1/Enter=改写、2=原文）与 Esc 取消后，后续打字正常
      （机器不失同步：字母不被吞、`r` 不误触发重试）
- [ ] 快速流切换 5 轮：旧流不混入（epoch 回归）

## // 命令（macOS 本批新接入，#89）
- [ ] `//截图` → verba-trigger ocr → 识别文本进 OCR 预览（与 `///` 同管线）
- [ ] `//听写` → verba-trigger asr（默认录 3s）→ 转写进预览
- [ ] `//朗读 <文本>` → TTS 合成并播放
- [ ] `//短语 <名称>` → 配置短语直插 + 剪贴板；未命中回退普通生成
- [ ] `//看图 <…>`：一期回退普通生成（无图附上，warn 日志），不崩不挂
- [ ] `//重置` / `//会话` 仍走 daemon（前端不拦截）

## 盲按队列（快打不漏字，#87）
- [ ] 同 Windows 批次四组快打组合（拼音+标点+标点 / +空格+标点 / +大写+标点 /
      断 daemon 到顶即时提交），慢速输入回归一致