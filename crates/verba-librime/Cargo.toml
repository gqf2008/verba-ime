[package]
name = "verba-librime"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "中文引擎（Rime/librime）：daemon 内动态加载（Windows rime.dll / macOS librime.dylib）"

# 本 crate 需要 FFI 动态加载，仅此处放开 unsafe（其余 crate 维持 workspace forbid）
[lints.rust]
unsafe_code = "allow"

[dependencies]
log.workspace = true
thiserror.workspace = true
libloading = "0.8"

[dev-dependencies]
